import { AutoresGeneroPipe } from './autores-genero.pipe';

describe('AutoresGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoresGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
